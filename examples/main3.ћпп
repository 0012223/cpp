// Example ћ++ program demonstrating language features
// Filename: main.ћпп

// External function declaration
екстерно испиши(порука);

// Array declaration and initialization
бројеви:4: = _1, 2, 3, 4_;

// Helper function that calculates the sum of an array
сума(низ::, н) <
    резултат = 0;
    и = 0;
    
    док (и < н) <
        резултат = резултат + низ:и:;
        и = и + 1;
    >
    
    врати резултат;
>

// Main function - entry point of the program
главна() <
    // Calculate sum of the array
    збир = сума(бројеви);
    
    // Print the result
    испиши(збир);
    
    // Return success code
    врати 0;
>